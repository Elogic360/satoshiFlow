{
  "name": "satoshiflow-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "axios": "^1.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
